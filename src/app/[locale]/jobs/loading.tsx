import React from 'react';
import { JobsPageSkeleton } from '@/components/ui/PageSkeletons';

/**
 * Loading state for Jobs Listing Page
 */
export default function Loading() {
  return <JobsPageSkeleton />;
}
